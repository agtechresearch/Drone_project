# 2026-09-23

**결정 (설계 확정)**
- 이격 50 cm(3조건 공통) · 마커 7 cm, **0.5 m 간격**(열당 12개, 하단 ID 0~11 / 상단 20~31) · 상단열 200 cm 설치
- 속도 0.5 m/s(v14 동일) · 이륙 후 **좌회전 90°**(현수막 좌측) · **복귀 구간 포함** · 정렬 실패 15 s → 착륙
- 허용 기준 5 cm, 조건 간 최소 차이 Δ 3 cm 는 잠정값. 허용오차·반복 수는 파일럿 데이터로 산정

**추가**
- `analysis/marker_drift/` 후처리 파이프라인: `coverage · calibrate · detect · noise · analyze · compare · layout` CLI.
  좌표계(M/T/C/L), IPPE_SQUARE PnP, NED→M heading 정합, 드리프트·경로 이탈·호버·잡음 지표, 표본 수 산정, 그림.
- `analysis/test_marker_drift.py` 70항목. aruco 로 렌더링한 tag36h11 합성 이미지를 pupil_apriltags 로 실제 검출해 검증.
- `docs/10_drone_survey_marker_drift.md` 기체 실측 조사.
- `.venv`(numpy·opencv·pupil-apriltags·pandas·matplotlib·scipy·pyyaml). 기체 SSH 공개키 등록, alias `starling2`(AP) / `starling2_lab`(랩망), `.env.local`.

**변경**
- 마커 15 cm → 7 cm 로 작아지며 단일 태그 PnP yaw 가 정면에서 튐(합성 최대 1.8°) → **태그 2개 이상 한 몸 PnP**(0.3° 이내) 도입.
  이를 위해 마커 간격 1 m → 0.5 m(전 구간 태그 2개 이상, 시작 위치에서 태그 0·1 동시 가시).
- 배치 생성기 버그 수정(간격 누적 오차, 상단열 ID 충돌).
- voxl-logger 로더 추가(`--log-format voxl-logger`). 행 끝 쉼표 → `index_col=False`.

**기체 조사 (읽기 전용, 8 s 시험 기록만 생성)**
- hires: `hires_small_color` 1024×768 / `hires_large_color` 4056×3040, 30 fps. 7 cm 태그 41 px / 164 px → **large 권장**.
- hires 캘리브레이션 파일 **없음** → 체커보드 필요. tracking_front 어안 캘리브레이션은 있음.
- `voxl-tag-detector` 설치·비활성. 태그 크기 0.4 m 설정 → 0.07 변경 필요. 기체 파이썬 3.6에 cv2·apriltag **없음** → 온보드 검출은 `tag_detections` 파이프 구독만 가능.
- voxl-logger: cam(jpg+data.csv) + ov + px4 pose + imu 동시 기록 확인, 모든 채널 CLOCK_MONOTONIC. 여유 61 GB.
- vision-hub `en_tag_fixed_frame` false(유지). 네트워크 sta+ap 동시(192.168.45.156 / 192.168.8.1).

**다음**
- 기체 없이: `tag_detections` 파이썬 구독기(252 B struct) + tracking_front 정렬 모듈 + 테스트
- 기체 접속 시: 7 cm 태그 손 검출 시험, hires 체커보드 캘리브레이션, large 기록 부하 시험, `tag_locations.conf` 변경(승인 후)
- 이후 v15 = v14 + `align_tag` 단계 + 로거 제어 + 단조시계 컬럼
