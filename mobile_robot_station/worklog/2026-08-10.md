# 2026-08-10 작업 기록 — 프로젝트를 Drone_project 저장소에 편입(이사+심볼릭 링크) + git/GitHub 관리 시작

## 수행 작업

### 1. 이전 작업 파악
- 마지막 세션(07-13) 확인: 데드밴드·맵·회전 파라미터는 처리됨. **핵심 미해결 = AMCL 위치추정 드리프트**(이동 후 정합 15cm/50%, 마지막 run은 스택 조기종료로 미측정 → 확정 아님). `escape_vel:-0.0` 미수정.

### 2. git/GitHub 편입 방식 결정
- 대상 저장소 = **`agtechresearch/Drone_project`**(모노레포, 컴포넌트별 최상위 폴더, 기본 브랜치 `main`, 서브모듈 미사용). 이 프로젝트는 "이동형 드론 스테이션 + 딸기 적재 모바일 로봇 자율주행" 컴포넌트로 편입.
- 결정: **이사 + 호환 심볼릭 링크** 방식. (대안이던 서브모듈/미러는 각각 관례변경·번거로움으로 제외.)
- ⚠️ 결정적 제약: 스크립트/코드 **24개 파일·33곳이 `/home/er/autonomous_nav` 절대경로 하드코딩** → 그냥 옮기면 로봇 안 됨 → 심볼릭 링크로 해결.

### 3. 이사 실행
- `sparse-checkout + --filter=blob:none`로 clone → 드론/시뮬 파일 미다운로드(252K만). `/home/er/Drone_project`.
- `autonomous_nav` → **`Drone_project/mobile_robot_station/`** 이동(같은 FS rename).
- **호환 심볼릭 링크**: `/home/er/autonomous_nav` → `Drone_project/mobile_robot_station` → 기존 33곳 절대경로·외부 참조 모두 그대로 작동(로봇 코드 무수정).
- 맵 심볼릭 링크(`current_map.*`) 절대→상대 경로화(이식성).

### 4. 문서화 + 커밋 + 푸시
- 추가: `PROJECT_STRUCTURE.md`(구조·역할·데이터흐름), `ROADMAP.md`(완료/할일), `.gitignore`(__pycache__·pyc·logs·scratchpad). 루트 README 표에 컴포넌트 등록. (기존 README/GUIDE/HOW_TO_RUN은 surgical 원칙으로 미수정.)
- 커밋 2개(`3f35983` 임포트 / `701ea39` 문서·README). feature 브랜치 → **main으로 fast-forward**(working tree 무변경 방식) → PR 없이 main 직접(팀 방식).
- 인증: PAT 대신 **SSH 키(ed25519)** 생성(`~/.ssh/id_ed25519`), 공개키 GitHub 계정 등록, 원격을 SSH URL로 전환. **push 성공**(`1487c7d..701ea39`).

## 주요 수치
- 커밋: `701ea39`(문서/README), `3f35983`(임포트). 76 files, +6165 insertions. 다른 컴포넌트 변경 0.
- 원격: `git@github.com:agtechresearch/Drone_project.git`, branch `main`. 로컬=원격 일치 확인.

## 결론
- 프로젝트가 이제 **Drone_project의 `mobile_robot_station` 컴포넌트**로 git 관리됨. 기존 4개 드론 컴포넌트는 무영향(트리·히스토리 그대로).
- 로봇 운용은 **`~/autonomous_nav/...` 경로 그대로** 가능(심볼릭 링크). 로봇 동작 로직은 한 줄도 안 바뀜.
- 앞으로: `git add` → `git commit` → `git push origin main` (SSH, 로그인 불필요). 큰 변경은 브랜치+PR 권장.

## 다음 할 일
- **(로봇 본업, 최우선)** 위치추정 드리프트 확정 진단 — 스택 켠 채로 목표1개(예냉실)→`loc_check.py`(≤15cm 비율)+`/move_base/GlobalPlanner/plan` 끝점+`/amcl_pose` vs 목표거리. (ROADMAP P1)
- 드리프트 확정 시: `escape_vel -0.0→-0.1` → `odom_alpha1`↑/파티클↑ → 목표간 재초기화 → AprilTag 재국소화. (P2)
- ⚠️ **다른 기기에서 clone 시**: `~/autonomous_nav` 심볼릭 링크 재생성 필요(스크립트 절대경로 때문). 근본적으론 경로 파라미터화가 이식성에 좋으나 지금은 보류(로봇 안정 우선).

## 인수인계 (이후 다른 담당자가 이어감)
- 08-10부로 이 프로젝트는 **다음 담당자에게 인계**. 진입점 문서 **`HANDOFF.md` 신규 작성**(문서 지도 / 현재 상태 / 당장 할 일 P1 / 함정 / git 워크플로우 / 환경).
- ⚠️ **진단 도구 유실 발견**: P1에 쓰던 `loc_check.py`·`map_quality.py`·`scan_vs_map.py`는 이전 세션 scratchpad 임시 파일이라 **저장소에 없음**(이미 유실). 다음 담당자가 재작성해야 하며, **추적 위치(`python/` 또는 `tools/`)에 두고 커밋**하도록 HANDOFF에 명시. `loc_check.py` 로직도 HANDOFF에 요약.
- 담당자 교체 시 git identity/SSH 키를 본인 것으로 바꿔야 커밋 귀속이 맞음(현재 이 Pi엔 직전 담당자 SSH 키만 설정) — HANDOFF §5에 안내.
