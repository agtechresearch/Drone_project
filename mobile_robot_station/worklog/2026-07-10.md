# 2026-07-10 작업 기록 — RViz 화면 안 뜸 원인 규명 (roscore 선행 + UI 타이밍) / 충전선 무관 확정

## 수행 작업

### 1. 초기 증상: `rosrun rviz rviz -d gmapping.rviz` 실행 시 "에러 나고 화면 안 뜸"
단계별로 원인 축소:
- **X 연결 정상**: `DISPLAY=:0 XAUTHORITY=/home/er/.Xauthority xset q` 성공, 창 매핑까지 확인
- **OpenGL 정상**: V3D 4.2 / Mesa 21.2.6 / GL 2.1 (하드웨어 가속 됨, GL 문제 아님)
- **진짜 원인 = roscore 없음**: rviz가 `"RViz: waiting for master"` 작은 창만 띄우고 메인 3D뷰를 안 올림
  - 포트 11311 미청취, `rostopic list` → `Unable to communicate with master!`, `rosmaster` 프로세스 없음
- 결론: **rviz는 뷰어라 마스터+노드가 먼저 떠 있어야 함.** 단독 실행하면 안 됨.

### 2. launch 구조 파악
- **주행(네비게이션)**: `autonomous_nav/launch/navigation.launch` (map_server+AMCL+move_base, 메카넘 파라미터)
  - 직접 X → 래퍼 `scripts/3_start_navigation.sh`로 실행 (①라이다전원 ②`myagv_odometry/myagv_active.launch` ③navigation.launch)
  - **rviz(dwa.rviz) 이미 포함**, `DISPLAY=:0` 자동. `rviz:=false`로 끌 수 있음(터치 UI가 이 옵션 사용)
- **매핑(SLAM)**: `scripts/1_make_map.sh` (라이다전원+오도+라이다+`gmapping.launch`, **rviz 없음**) → `map_teleop.sh`(단일키 조종) → `2_save_map.sh`
- ⚠️ **`1_slam_mapping.sh` 함정**: 부르는 `myagv_slam_laser.launch`가 **gmapping+rviz만** 포함, 오도/라이다 드라이버 빠짐(스크립트 주석과 불일치). 단독 실행 시 `/scan`·`/odom` 없어 **맵 안 그려짐.** → 매핑은 반드시 `1_make_map.sh` 사용.
- rviz 설정 용도: `gmapping.rviz`=매핑용, `dwa.rviz`=주행용

### 3. 터치 UI(바탕화면 "로봇 주행 UI" 아이콘) 구조 + RViz 버튼
- 아이콘(`~/.local/share/applications/robot-ui.desktop`, `~/Desktop/robot-ui.desktop`) → `scripts/robot_ui_launch.sh` 실행
- `robot_ui_launch.sh`: move_base 미기동 시 `3_start_navigation.sh --no-rviz` 백그라운드 시작 → `waypoint_ui.py` 실행 → 종료 시 `stop_all.sh` 정리
- `waypoint_ui.py`: `toggle_rviz()` + "RViz 열기/닫기" 버튼 존재(dwa.rviz, `DISPLAY=:0`/XAUTHORITY 지정 subprocess). **즉 UI에서 rviz 켜는 기능은 이미 구현돼 있음** — 수동 rosrun 불필요.
- **RViz가 안 뜨던 진짜 원인 = 타이밍**: `robot_ui_launch.sh`가 네비 시작 후 **`sleep 3`만** 하고 UI를 띄우는데, 이 Pi에선 스택 준비에 **15~25초** 필요. 준비 전 RViz 누르면 마스터/지도 없어 빈 화면/waiting.
- **사용자 검증**: UI 켜고 시간 지난 뒤 RViz 누르니 정상 작동 확인 ✅

### 4. 충전선 판정 → 원인 아님 (로그 + 라이브 확인)
- `robot_ui_nav.log`: 네비 스택 **정상 기동**(마스터 자동시작→지도 로드 544×544→라이다 스캔→`odom received!`→move_base 준비). 65초 뒤 `[user request]` 셧다운 = **UI 닫아서 stop_all.sh 정리**(에러 아님)
- 라이브 확인(현재 gmapping 세션): `/scan`(라이다) `/odom`(오도) **실시간 발행 정상**
  - ※ `rostopic hz`는 6초 타임아웃이 짧아 rate 못 뽑음(연결설정 지연) → `rostopic echo -n1`에 12초 주니 실제 메시지 수신 확인
- ⚠️ **myAGV 알려진 동작**: 충전선 꽂으면 **바퀴 구동(모터)만 비활성**, 오도/라이다는 계속 나옴. → RViz 시각화엔 무해, **실제 주행 테스트 땐 충전선 제거 필요.**

## 주요 수치
- OpenGL: V3D 4.2 (Broadcom), Mesa 21.2.6, GL 2.1
- 맵: `map_20260707_154005` 544×544 @ 0.05 m/pix (current_map.yaml 링크 유효)
- 라이다: `/dev/ttyAMA0` @ 115200 (YDLidar X2, SDK 1.1.15), 오도 보드 시리얼 별도
- UI 대기시간: 현재 `sleep 3` / 스택 실제 준비 15~25초

## 결론
- rviz 단독 실행 실패 = roscore 미기동. rviz는 마스터+데이터 노드 선행 필수.
- 터치 UI RViz 버튼 실패 = 스택 준비 타이밍 문제(3초 대기 부족). **기다리면 정상 작동**(검증됨).
- 충전선은 roscore/데이터/rviz와 무관. 구동만 막음(주행 테스트 시 제거).
- 실시간 데이터(맵+로봇위치+라이다)는 하드웨어(라이다+오도) 필수 — "실제 주행 안 함"이어도 센서 스택은 다 떠야 함.

## 다음 할 일
- **재매핑 진행**: `1_make_map.sh` + `map_teleop.sh`로 새 맵 작성 → `2_save_map.sh` → current_map 갱신 → 예냉실 재주행
- 재주행 시 초기위치 설정(2D Pose Estimate) 필수 — 07-09 미해결 이슈(AMCL (0,0,0) 방치 시 move_base ABORTED)
- 주행 테스트 시 **충전선 제거**
- (선택/미적용) `robot_ui_launch.sh`의 `sleep 3` → `/map` 토픽 뜰 때까지 대기(최대 30s)로 개선하면 RViz 아무 때나 눌러도 정상. 사용자 판단 대기.

---

## (오후 세션) 재매핑 완료 → 맵 품질 점검 → 초기위치 픽스 → set_initialpose.sh 제작

### 1. 재매핑 완료
- 새 맵 `maps/map_20260710_183322` 저장, `current_map` 링크 갱신.
- 전체 방 안 돌고 경로 위주로 매핑(목표대로 시간 단축).

### 2. 맵 품질 점검 (스크립트로 정량 분석)
- 도구: `scratchpad/map_quality.py` (PGM 로드 → 셀통계 + 풋프린트 팽창 후 연결성 BFS + choke 탐지 + 시각화)
- 결과: **양호.** 실제 매핑범위 ~6.2×12.9m, free 46.7㎡.
  - 벽만 고려 시 free **1덩이**(막힌 방 없음), 풋프린트(inscribed 0.12m≈2px) 고려 시 시작점에서 **35.9㎡ = 전체 주행가능의 97% 도달.**
  - "좁은 길목" 경고는 0.2㎡ 자투리 한 조각(가장자리 미완 개구부) → 오탐, 주행 무관.
  - **지난번(1.3㎡ 방 갇힘)과 완전히 다름.** 시각화: `~/Pictures/map_quality_20260710.png`(+_crop).

### 3. 라이다 vs 맵 정합 점검 → 원인은 맵 아니라 initialpose 미설정
- 도구: `scratchpad/scan_vs_map.py` (라이브 /scan을 tf2로 map 투영 → 정적맵 벽과 거리 + 8방위 라이브vs맵레이캐스트 비교). **각도 규약 혼동 피하려 tf2 전체 체인 사용.**
- 초기위치 설정 전: 중앙값 33cm, ≤15cm 7%, `/amcl_pose` 없음, 좌/우 오차 반대부호(회전·평행 오프셋 전형) → **AMCL (0,0,0) 방치**.
- 로봇을 원점에 물리 배치 후: 중앙값 **5cm, ≤15cm 90%, ≤30cm 100%**, 좌90/우-90 거의 완벽(헤딩 정확) → **맵 정상 확정, 문제는 위치추정뿐.** (07-09 반복 이슈)
- `rostopic pub -1 /initialpose (0,0,0)` 발행 → `/amcl_pose` 즉시 뜸(x=0.04,yaw≈-1°, cov 정상). **누가 발행하든 같은 마스터라 동일.**

### 4. set_initialpose.sh 제작 (RViz 없는 초기위치 설정)
- `python/set_initialpose.py` + `scripts/set_initialpose.sh` (실행권한 부여, 테스트 통과).
- `set_initialpose.sh [x] [y] [yaw°]` (기본 원점). yaw°→quaternion 자동변환, 발행 후 `/amcl_pose` 반영·요청값 일치까지 확인.
- **RViz 2D Pose Estimate 대체** (Pi GPU/RAM 부담으로 RViz 불가). GUIDE.md B안 시작방법에 반영함.

### 5. 정밀 이동 수단 정리 (RViz 없이, 오도 피드백)
- `move.sh forward/back/left/right/turn <값>`(정확한 거리/각도, direct 제어, nav 중에도 동작), `map_teleop.sh`(단일키 스텝). 방향 정밀지정은 move.sh turn.
- ⚠️ save_waypoint는 `/amcl_pose` 필요(없으면 odom 폴백=무효). go.sh 도착정밀도는 goal tolerance 0.15m/8.6°에 묶임.

## (오후 세션) 다음 할 일
- **예냉실 정밀 이동 → save_waypoint.sh 예냉실 (로그 "(map)" 확인) → go.sh 예냉실 검증** (재주행 성공 여부 = 이번 재매핑의 최종 검증).
- 옛 waypoints.yaml은 옛 맵 좌표라 무효 → 새 맵에서 재저장/정리 필요.
- 매 세션 절차: 로봇 원점 배치 → `set_initialpose.sh` → 주행.

## 개선점 (기록만, 지금은 미적용 — 먼저 waypoint 이동 동작 확인 우선)
원점 set_initialpose 후 로봇을 수동으로 ~4m 이동(→ AMCL (1.36,4.03,108°)) 후 정합 점검 결과:
- **정합 느슨해짐**: 원점 median 5cm/≤15cm 90% → 이동 후 **14cm / 50%** (≤30cm는 90%). 우측 벽은 정렬 OK, 좌측 흩어짐. 오버레이: `~/Pictures/scan_overlay_20260710.png`.
- 심각한 오차 아님: 실제 14cm ≈ AMCL 공분산(std~9cm)의 1.5σ. 즉 로봇은 대략 맞는 위치, 다만 원점만큼 크리스프하진 않음.
- **원인 추정**: (1) 수동 ~4m 이동 중 오도 드리프트(메카넘 횡/회전 슬립)를 AMCL이 부분만 보정, (2) 이 구역 맵 품질이 원점보다 약간 낮을 가능성. AMCL이 과확신(공분산 작음)이라 가만두면 자가보정 잘 안 됨.

**추후 개선(우선순위 순):**
1. 정밀 waypoint 필요 시: 특징 뚜렷한 지점(원점 등)에서 `set_initialpose` → **천천히·전진 위주로** 이동(급횡이동/급회전 자제) → save 전 `scan_vs_map`로 ≤15cm 80%+ 확인하는 절차 습관화.
2. 특정 구역 정합이 계속 나쁘면 그 구역 **맵 보강 재매핑**.
3. 좌90 방위에서 라이브 2.76m(개방) vs 맵 0.60m(벽) → (1.4,4.0) 부근 **맵 가짜 벽(아티팩트) 의심** — 경로에 걸리면 planner가 유령 장애물 회피할 수 있음. 미확인, 필요 시 맵 이미지 점검/편집.
4. (기존) `robot_ui_launch.sh` `sleep 3` → `/map` 대기로 개선.
