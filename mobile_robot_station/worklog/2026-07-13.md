# 2026-07-13 작업 기록 — 배터리 전압 진단 / 지난 주행실패 원인(메카넘 데드밴드) 복기 / 공식 GitHub 조사(벤더=diff-drive 설계)

## 수행 작업

### 1. 배터리 전압 진단 (ROS 없이)
- ROS 마스터 미기동 상태 → 시리얼(`/dev/ttyAMA2`)에서 직접 읽기 시도했으나 **raw 0바이트**.
- 원인 = **MCU 자동 리포트(auto_report)가 OFF**. 포트·권한(dialout)·프로세스 점유 모두 정상이었음.
- `pymycobot.MyAgv('/dev/ttyAMA2',115200)`로 능동 진단:
  - `get_firmware_version()` → **1.2** (MCU 살아있음 확인)
  - `get_auto_report_state()` → **0(off)** → `set_auto_report_state(1)`로 켬
  - `get_mcu_info()` → **메인 배터리 11.1~11.2V** (index 16 = C++ `buf[16]/10`과 동일 위치)
- auto_report ON 후 raw 스트리밍 정상 복구(2초 1152바이트) → ROS 노드 데이터 수신 가능 상태.
- ⚠️ **"충전 중"인데 전압이 안 오름**: 6초 관찰 11.1→11.2→11.1V로 평평, 07-09의 11.4V보다 오히려 낮음. 07-07과 동일하게 **실제 충전이 안 되고 있을 가능성**(충전기/접점 의심). 11.1V는 모터 구동엔 충분(07-09 11.4V에서 주행 성공).

### 2. 지난 주행실패 원인 복기 → 메카넘 저속 mix 데드밴드
- 사용자가 "마지막 실패 원인 = x·y축 이동 명령이 동시에 안 됐다"로 기억. **worklog/메모리엔 이 내용 없음** → 코드에서 재확인.
- `python/go_simple.py` docstring에 명시: **"메카넘 저속 mix(전진+회전 동시)가 모터 데드밴드에 걸리는 문제를 우회한다. 로봇이 확실히 하는 동작(순수 회전/순수 직진)만 사용."**
- 속도 변환 `byte = v×100+128` (C++ `myAGV.cpp:230` writeSpeed). v=0.02 m/s면 byte=130(중앙 128에서 +2) → 모터가 못 냄 = 데드밴드. move_base가 목표 근처에서 vx·vy·회전을 잘게 섞어 발행 → 각 성분이 데드밴드 아래 → 로봇 정지.
- **`go_simple.py` 생성 시각 = 07-10 20:56.** 그날 오후 worklog는 19:35 종료 → **07-10 저녁에 이 문제를 발견하고 대응한 세션이 정리 안 됨**(사용자만 기억, 내 기록엔 공백).

### 3. 맵 자율주행 코드 분류 (`autonomous_nav/python`)
- **move_base/DWA 계열**(장애물 회피 O, 저속 mix 데드밴드 취약): `waypoint_ui.py`(버튼), `nav_goal_sender.py`(go.sh 호출), `move_relative.py`, `patrol_navigator.py`
- **move_base 우회**(데드밴드 회피, 장애물 회피 X): `go_simple.py`(AMCL 위치 기반 회전→직진)
- 보조: `save_waypoint.py`, `set_initialpose.py`, `robot_status.py`, `camera_obstacle.py`
- `/home/er/pymycobot` = **Elephant Robotics SDK 소스 저장소**(명령 라이브러리). 자율주행 코드 없음. 자율주행은 `autonomous_nav/` + `myagv_ros/`에만.

### 4. 공식 GitHub 조사 → 벤더는 메카넘을 diff-drive로 운용
- 공식 저장소 = **github.com/elephantrobotics/myagv_ros** (ROS1) / myagv_ros2 (ROS2). **로컬 `/home/er/myagv_ros`가 바로 이것**(패키지 구성 동일). 공식 자율주행 코드는 이미 기기에 있었음 = 표준 ROS 네비 스택.
- `myagv_navigation`은 로컬 플래너 3종 제공: `base_local_planner/TrajectoryPlannerROS`(벤더 기본), `dwa_local_planner/DWAPlannerROS`, `teb_local_planner/TebLocalPlannerROS`.
- **벤더 기본 `base_local_planner_params.yaml`: `holonomic_robot: false`, `acc_lim_y: 0.0`, `min_in_place_vel_theta: 0.25`.** DWA 기본도 `max_vel_y: 0.0`, `vy_samples: 0`. → **벤더는 메카넘인데도 자율주행에선 일부러 횡이동/동시축을 안 쓰고 차동구동(회전 후 직진)처럼 몰음.**
- **데드밴드 원인 확정**: 이전 커스텀 `config/mecanum_dwa_params.yaml`이 벤더가 꺼둔 **y축을 켜고**(`max_vel_y:0.15`, `vy_samples:4`) **최소속도까지 낮춤**(`min_vel_trans:0.05`) → x+y+회전 mix가 데드밴드로.
- **현재 상태(07-10 저녁 수정, 이미 반영됨)**: `autonomous_nav/launch/navigation.launch`가 **벤더 `TrajectoryPlannerROS`로 전환**(라인 52) + `config/navigation_overrides.yaml`(costmap만 오버라이드). `mecanum_dwa_params.yaml`은 미사용(레거시). go_simple.py도 같은 "mix 없음" 결론.

## 주요 수치
- 배터리: **11.1~11.2V** (07-09 11.4V, 07-07 10.3~10.4V). 완충 추정 12.6V / 저전압 컷오프 추정 10.5V.
- MCU 펌웨어 1.2. auto_report 기본 OFF였음(전원 사이클 후 추정).
- 벤더 base_local_planner: holonomic_robot=false, max_vel_x 0.2, min_vel_x 0.01, max_vel_theta 0.1, min_in_place_vel_theta 0.25, acc_lim_y 0.
- 커스텀 mecanum_dwa(레거시): max_vel_y 0.15, min_vel_y -0.15, vy_samples 4, min_vel_trans 0.05.
- go_simple.py 생성 2026-07-10 20:56.

## 결론
- 지난 주행실패 최종 원인 = **메카넘 저속 다축 mix가 모터 데드밴드에 걸림.** 벤더 설계는 `holonomic_robot:false`로 이를 회피(diff-drive식 운용).
- 이 수정은 **07-10 저녁 세션에서 이미 코드에 반영**(navigation.launch→벤더 플래너, go_simple.py)됐으나 **실차 검증·기록이 안 됐음.** 지금 버튼 테스트가 그 검증.
- 배터리: auto_report OFF면 `/Voltage`(및 ROS 오도)가 안 나올 수 있음 → pymycobot로 켜야. 현재 11.1V(주행 가능), 단 충전 실효 의심.

## 다음 할 일
- **충전 실제 여부 확인**: 몇 분 관찰해 전압 상승하는지. 안 오르면 충전기/접점 점검.
- **주행 테스트(충전선 제거)**: 실증 현장 짧은 새 맵 매핑(`1_make_map.sh`+`map_teleop.sh`→`2_save_map.sh`) → 원점 `set_initialpose.sh` → `save_waypoint.sh`(로그 "(map)" 확인) → **터치 UI 버튼 주행** = 벤더 diff-drive 플래너(TrajectoryPlannerROS) **실차 최초 검증**.
- 버튼이 여전히 데드밴드로 안 움직이면: (a) 벤더 min_in_place_vel_theta/min_vel_x 더 올리기, (b) `go_simple.py` 방식을 UI 버튼에 연결(장애물 회피 없음 감수), (c) TEB 플래너 시도.
- 옛 `waypoints.yaml`은 옛 맵 좌표라 무효 → 새 맵에서 재저장.

---

## (실증 현장 세션) 새 맵 제작 → 버튼 주행 → 위치추정 드리프트 진단 → AMCL 1단계 튜닝

### 1. 새 맵 + 웨이포인트
- 처음에 `1_make_map.sh` 대신 네비 스택을 띄우는 실수(gmapping 없이 옛 맵 로드) → 바로잡아 재매핑.
- 새 맵 **`map_20260713_111502`**(current_map 링크됨), 자유공간 **37.9㎡**(x[-4.5,7.4] y[-3.4,4.65]), 갇힘 없음.
- 웨이포인트 2개 모두 새 맵 자유공간에 유효: **Room1**(-0.30,-0.11,176°), **예냉실**(3.00,-0.53,97°). (주변 25/25 자유셀)

### 2. 버튼 주행 = 데드밴드는 해결, 그러나 2번째 목표부터 이상행동
- UI에 **초기위치 팝업 + `⌂ 원점 초기화` 버튼**이 이미 있음(07-10 20:08 추가분, 내 기록엔 "미구현"으로 잘못 남아 있었음 → 정정). `set_initialpose.sh` 따로 불필요.
- ⚠️ **타이밍**: 팝업이 창 뜨고 0.6초에 뜨는데 스택 준비 15~25초 → 너무 일찍 [예] 누르면 amcl이 `/initialpose` 미구독이라 발행 유실. **"준비됨" 뜬 뒤 `⌂ 원점 초기화` 버튼** 권장.
- 원점 초기화 직후 `/amcl_pose` (-0.03,0.008), yaw≈0.2° → 정상. **첫 목표 주행 성공**(데드밴드=벤더 diff-drive 플래너로 해결 확인 ✅).
- **증상**: 첫 목표 도착 후 2번째 목표부터 버벅임/엉뚱한 경로/실벽 충돌.

### 3. 원인 = AMCL 위치추정 드리프트 (라이브 확인)
- `scratchpad/loc_check.py`(tf2로 /scan을 map 투영 → 정적맵 벽 거리 + amcl 공분산): **중앙값 34cm, ≤15cm 21%** (07-10 원점 5cm/90% 대비 급락). AMCL 공분산 std x=9cm,y=8cm,yaw2.9° → **과확신인데 실제 34cm 오차**.
- move_base 로그: `Failed to get a plan`×6, `NO PATH!`×2, **`Failed to get a plan from potential when a legal potential was found. This shouldn't happen.`×2**(출발셀이 유령벽 안=위치 어긋남 신호), 복구행동 costmap clear 발동.
- **자기참조 함정**: move_base 도착판정이 AMCL 추정 기준이라, 드리프트 시 로봇은 물리적으로 waypoint에 안 가 있음(≈34cm 밖). 그래서 "도착 시 waypoint 좌표로 AMCL 리셋"은 효과 없음(이미 W라 믿는 값을 다시 W로 = 변화 없음, 실제 오차 34cm 그대로).
- **보조 버그**: `base_local_planner_params.yaml`의 `escape_vel: -0.0`(=0) → 복구 시 로봇이 뒤가 아니라 앞(장애물 쪽)으로 감. `escape_vel:-0.1`로 고치면 충돌 완화(미적용, 별건).

### 4. AMCL 1단계 튜닝 적용 (navigation.launch)
- 확인 중 발견: launch가 이미 `transform_tolerance 1.5`, `min/max_particles 500/3000`으로 오버라이드 중(yaml값 아님) → 제안했던 transform_tolerance·파티클 변경 **불필요/제외**.
- **실제 추가한 것**(amcl 노드 `<param>`): `odom_alpha2 0.3→0.4`, `odom_alpha4 0.1→0.25`(회전기인 오차↑=메카넘 슬립 반영), `update_min_d 0.15→0.10`, `update_min_a 0.2→0.15`(보정 빈도↑). 의도: 오도 과신 완화 → 파티클 덜 오그라들어 스캔 재보정 살리기.
- ⚠️ **재시작해야 반영**: `stop_all.sh` → `robot_ui_launch.sh` → "준비됨" 후 원점 초기화 → 재주행. 검증: `loc_check.py`로 이동 후 정합이 ≤15cm 비율 개선되는지.

### 5. 향후: 마커 기반 waypoint 재국소화 (기록용, 미구현)
사용자 아이디어 확장 — **바닥 마커/AprilTag/QR을 카메라로 인식해 그 지점의 맵 좌표로 AMCL을 리셋**하면, 자기참조 함정 없이 "도착 지점에서 정확한 위치 동기화"가 가능. 실제 산업 AGV 표준 방식(도킹/피듀셜). 이 로봇은 카메라가 있으나(라인추종/장애물용) 마커 인식이 파이프라인에 없음 → **카메라 재활용해 AprilTag 재국소화 노드 추가가 유력한 확장 후보.** 튜닝으로 상시 정합을 개선하되, 근본 한계(후방 라이다 사각+메카넘 슬립)는 마커/360°라이다 같은 독립 기준으로만 완치.

## (실증 세션) 다음 할 일
- 튜닝 반영 재시작 → 2번째·3번째 목표까지 정합 유지되는지 `loc_check.py`로 검증(≤15cm 80%+ 목표).
- 개선 부족 시: `odom_alpha1` 추가 상향 검토, `escape_vel -0.1` 적용, 그다음 AprilTag 재국소화 설계.

### (추가) AMCL 튜닝 검증 + 회전 파라미터 수정
- **1단계 AMCL 튜닝 검증됨**(재시작·원점초기화·예냉실→Room1 왕복): 정합 중앙값 **34→15cm, ≤15cm 21→50%**, move_base `Failed to get a plan`/`NO PATH`/복구행동 **0건**(지난번 다수) → 계획실패 증상 해소. rosparam으로 alpha2 0.4/alpha4 0.25/update_min 0.10·0.15 로드 확인.
- **잔여 증상 = 최종 yaw 회전 버벅임/헤맴**(실패는 없음). 원인: 로드값 `max_vel_theta 0.1 < min_in_place_vel_theta 0.25`(모순, 제자리회전이 캡에 걸림) + `latch_xy_goal_tolerance false`(위치 도달 후 방향 돌리다 목표점 왕복).
- **수정 적용**(`config/navigation_overrides.yaml`에 `TrajectoryPlannerROS:` 섹션 추가): `max_vel_theta 0.5`, `min_vel_theta -0.5`, `latch_xy_goal_tolerance true`. theta만 조정(병진 무관). `acc_lim_theta`는 보류. **재시작해야 반영** → 회전 버벅/헤맴 감소 확인 예정.

### (회전 수정 후 재주행 — 미완, 다음 세션에서 이어서 분석)
- 회전 파라미터 반영 확인됨(rosparam: `max_vel_theta 0.5`, `min_vel_theta -0.5`, `latch true`).
- **결과: 목적지 도달 실패 + 경로가 안 맞아 보임**(사용자 관찰). move_base status **2(PREEMPTED=목표 취소)** — 스택 종료/정지로 취소된 상태.
- **로그 분석(`/tmp/robot_ui_nav.log`)**: 이번 주행엔 `Failed to get a plan`·`NO PATH`·런타임 oscillation·recovery·abort **전부 0건**(startup의 escape_vel 경고만; oscillation/escape grep 3·4건은 전부 파라미터 덤프 오탐으로 확인). → **계획 레벨 실패는 없었음.**
- ⚠️ **이번 주행 라이브 정합(loc_check)은 측정 못 함**: 내가 확인하러 갔을 때 이미 **스택이 내려가(마스터 다운)** 있었음. 따라서 이번 run의 위치추정 수치는 없음.
- **잠정 결론**: 계획 실패가 없는데 "도달 못 함 + 경로 이상"이면, 로봇이 **틀어진 자기위치 기준으로 유효한 경로를 그려 엉뚱한 물리 위치로 감** = **위치추정 드리프트**가 가장 유력(직전 run에서 이미 15cm/50%로 crisp하지 못했음). 단 **이번 run은 미측정이라 확정 아님.**
- 회전 버벅임 감소 여부도 **결론 못 냄**(로그에 회전 이벤트 없고 육안 판단만; 목표 도달 실패로 회전 단계까지 제대로 못 감).

## ★ 다음 세션 재개 지점 (여기서 시작)
1. **스택을 켠 채로 진단**(중요: 이번엔 loc_check 돌리기 전에 stop_all 하지 말 것). `robot_ui_launch.sh` → 준비 후 `⌂ 원점 초기화` → **목표 하나(예냉실)만** 전송.
2. 주행 중/직후 **스택 살아있는 상태에서**:
   - `loc_check.py`로 라이브 정합 측정(≤15cm 비율) → 드리프트 확정/부정
   - 라이브 글로벌 플랜 확인: `rostopic echo -n1 /move_base/GlobalPlanner/plan`(또는 `/move_base/*/global_plan`) 끝점이 실제 목표와 맞는지 → 경로가 엉뚱하면 드리프트 확정
   - `/amcl_pose` vs 목표 좌표 거리
3. **드리프트 확정 시 순서**: (a) `escape_vel -0.0 → -0.1`(복구 시 전진→충돌 방지, `base_local_planner_params.yaml` 또는 overrides에), (b) `odom_alpha1` 상향/파티클↑, (c) 목표 간 재초기화 운용, (d) **AprilTag 재국소화**(카메라 재활용, 근본).
4. 회전 버벅임은 **도달이 되는 조건에서** 재판정.

**핵심 미해결**: 위치추정 드리프트(이동 후 정합 느슨 + AMCL 과확신). 데드밴드·맵·회전파라미터는 처리됨. escape_vel(-0.0)은 아직 미수정.
